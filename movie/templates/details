<!DOCTYPE html>
{% extends 'base.html' %}
{% load static %}
{% block content %}
<style>
.card {
  background-color: #fff;
  }
</style>
<h2 class="text-center mt-3">BOOK DETAILS</h2>
<!--{{books.title}}-->
<!--{{books.author}}-->
<!--{{books.language}}-->
<div class="card mb-3 mx-auto p-4" style="max-width: 740px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{movie.image.url}}" class=" rounded-start" height="340px" width="100%">
    </div>
    <div class="col-md-8">
      <div class="card-body ms-5">
        <h5 class="card-title text-secondary mb-3 fw-bold">{{movie.title}}</h5>
        <p class="card-text"><b>Title : </b>{{movie.title}}</p>
        <p class="card-text"><b>Description: </b>{{movie.description}}</p>
        <p class="card-text"><b>Language : </b>{{movie.language}}</p>
        <p class="card-text"><b>Year : </b>{{movie.year}}</p>
        <a href="{% url 'edit' movie.id %}" class="btn btn-outline-secondary mt-3">Edit</a>
        <a href="{% url 'delete' movie.id %}" class="btn btn-outline-secondary mt-3 ms-5">Delete</a>
      </div>
    </div>
  </div>
</div>
{% endblock %}